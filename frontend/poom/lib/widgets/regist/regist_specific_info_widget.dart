import 'package:flutter/material.dart';

class RegistSpecificInfo extends StatelessWidget {
  const RegistSpecificInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
      child: const Text('보호견 상세 등록'),
    );
  }
}
